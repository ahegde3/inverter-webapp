const config = {
  plugins: ["@tailwindcss/postcss"],
  // Alternative config if lightningcss issues persist:
  // plugins: {
  //   tailwindcss: {},
  //   autoprefixer: {},
  // },
};

export default config;
